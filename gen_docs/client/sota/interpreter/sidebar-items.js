initSidebarItems({"struct":[["CommandInterpreter","The `CommandInterpreter` wraps each incoming `Command` inside an `Interpret` type with no response channel for sending to the `GlobalInterpreter`."],["EventInterpreter","The `EventInterpreter` listens for `Event`s and optionally responds with `Command`s that may be sent to the `CommandInterpreter`."],["GlobalInterpreter","The `GlobalInterpreter` interprets the `Command` inside incoming `Interpret` messages, broadcasting `Event`s globally and (optionally) sending the final outcome `Event` to the `Interpret` response channel."]],"trait":[["Interpreter","An `Interpreter` loops over any incoming values, on receipt of which it delegates to the `interpret` function which will respond with output values."]]});